name: base256
version: "0.1.0"
about: Encode and decode data in base 256
args:
        - action_decode:
                short: d
                long: decode
                help: Decode data (default action is to encode data).
